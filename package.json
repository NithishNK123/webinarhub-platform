{
  "name": "webinarhub-platform-root",
  "version": "1.0.0",
  "description": "Root orchestrator for PaaS deployments (Render/Heroku/Railway)",
  "scripts": {
    "install": "cd backend && npm install",
    "build": "cd backend && npm run build",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
